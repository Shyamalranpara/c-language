#include<stdio.h>
int main(){
    int x=8;
    int y=3;

    if (x < y)
    {
        printf("enter the first number : %d\n",x);
        printf("enter the second number :%d\n",y);
    }
    else
    {
        printf("the minimum value is :%d",y);
    }
    
}